# BEHAVIORS — 行为发现记录

此文件在探查目标站点（如 https://adxkit.com）时用于记录观察到的所有交互行为：

- 滚动驱动的动画、IntersectionObserver 触发器
- 导航栏在滚动时的外观变化（高度、阴影、背景）
- 进入视口动画（淡入、上移、延迟序列）
- 悬停效果（按钮、图卡、链接）
- 点击交互（标签页、弹窗、下拉菜单）
- 响应式布局的断点行为

在开始构建前，请使用浏览器 MCP 按照 `scripts/` 中的提取脚本逐项执行，并把结果追加到本文件中。