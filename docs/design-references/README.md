Design references (screenshots and crops) used as visual targets for builders.

Place full-page and component screenshots here with descriptive filenames, for example:
- `adxkit-desktop-1440.png`
- `adxkit-mobile-390.png`
- `hero-section-crop.png`

Screenshots should be captured via browser MCP at the specified viewport sizes: 1440px (desktop), 768px (tablet), 390px (mobile).